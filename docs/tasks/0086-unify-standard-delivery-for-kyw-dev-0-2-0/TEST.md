# TEST 0086 — Unify STANDARD Delivery for kyw-dev 0.2.0

<!-- kyw-task-contract: 4 -->

## Status

PASSED

## Test Basis

- Task: `./TASK.md`
- Product requirements: `../../SPEC.md`, especially explicit Skill routing, Task/Test contracts, `STANDARD` delivery, publication authority, safety, compatibility, version truth, and acceptance.
- Architecture constraints: `../../ARCHITECTURE.md`, especially the one-adapter dependency graph, delivery/public-state flow, external ledgers, continuity isolation, trusted publication workflow, package boundary, and validation.
- Repository rules and usage: `../../../AGENTS.md`, `../../../README.md`, `../../../CODEX_PROMPTS.md`, and `../../../templates/project/AGENTS.md`.
- Hard dependency and immutable safety baseline: Task 0085.
- Canonical contract and runtime surfaces: `../../../templates/task/`, `../../../skills/kyw-deliver/`, `../../../skills/kyw-task/`, `../../../skills/kyw-impl/`, `../../../src/core/`, and `../../../scripts/lib/validate-foundation.mjs`.
- Version, distribution, and external-boundary surfaces: `../../../package.json`, `../../../.codex-plugin/plugin.json`, `../../../.github/workflows/publish.yml`, installer/package inventory, and current public npm/GitHub read-only state.

## Model Provenance

- Model identifier: `UNAVAILABLE` (`UNAVAILABLE`: the active API surface does not expose the exact configured model identifier)
- Requested model alias: `NOT_REQUESTED` (`OBSERVED`: the current user requested no model override)
- Reasoning effort: `UNAVAILABLE` (`UNAVAILABLE`: the active API surface does not expose the configured reasoning effort)
- Codex surface: `API` (`OBSERVED`: the current system identifies access through an API)
- Codex version: `UNAVAILABLE` (`UNAVAILABLE`: the active API surface does not expose its Codex version)

## Intent-to-Test Matrix

| ID | Intent / acceptance criterion | Method | Level | Status | Evidence |
|---|---|---|---|---|---|
| T-01 | AC-01 — exact plain route is sole authority and suffix is unsupported | Parse and dispatch the exact positive plain form plus `--public-release`, arbitrary suffixes, aliases, prose, implicit, chained, continuous, and background inputs; assert rejected forms have no delivery/public mutation authority and advertise only the plain form. | Routing / negative | PASS | Parser, dispatcher, Skill, metadata, and direct behavioral fixtures accept only the exact plain form and reject the retired suffix and every alternate form before mutation. |
| T-02 | AC-02 — one invocation crosses the full STANDARD and public sequence | Drive every pending Git/GitHub stage, exact `STANDARD FINAL`, and already-final state through the Skill/adapter boundary; prove ordered commit through post-main resume followed in the same invocation by npm, tag, and Release without a second prompt or command. | Workflow / integration | PASS | Injected lifecycle traces prove the plain route completes/revalidates every STANDARD stage and then enters npm, tag, and Release in the same invocation after exact `FINAL`. |
| T-03 | AC-03 — contract 4 requires prospective STANDARD release versions compatibly | Validate new templates and authored pairs with exact, missing, malformed, duplicate, prerelease, `NONE`, and terminal version cases; prove contracts 3 and 4 both retain immutable-terminal handling, keep versionless contract 1–3 plain delivery GitHub-only/report-only, and reject a future contract-4 `STANDARD DONE/PASSED` pair without the required version. | Contract / compatibility | PASS | Template, artifact, queue, authoring, and implementation suites cover valid and invalid contract-4 versions, reasoned `NONE`, immutable contracts 3/4, and versionless contract 1–3 compatibility. |
| T-04 | AC-04 — every current version projection agrees on 0.2.0 | Inspect package/plugin manifests, CLI version, foundation constants, installer metadata, packed bytes, install examples, README/SPEC wording, and historical facts; perform fresh cache-bypassed target-absence checks before edits and terminalization, including bounded canonical dispatch-marker proof for every matching publication attempt. | Version / distribution | PASS | Distribution, installation, CLI, foundation, instruction, and pack checks agree on source version `0.2.0`; fresh canonical reads at `2026-09-04T07:07:39Z` prove the public target remains absent. |
| T-05 | AC-05 — version is Task-owned before delivery and immutable during delivery | Mutate the Task release version, package/plugin versions, registry history, matching workflow attempts, tag/Release target, current-queue duplicate claim, and tuple version independently; assert missing selection blocks authoring, implementation blocks conflict, and delivery only validates the unchanged exact field without any version write path. | Identity / boundary | PASS | Contract, queue, hydration, dispatcher, and public-core fixtures block missing, duplicate, occupied, mismatched, or changed versions and consume only the immutable Task-owned value. |
| T-06 | AC-06 — STANDARD-first frozen tuple and five-state preflight stay exact | Exercise nonterminal and final `STANDARD` graphs and mutate each frozen tuple/public surface field; require zero public calls before exact `FINAL` and closed `ABSENT`, `EXACT_ALREADY_COMPLETE`, `PENDING_PROOF`, `CONFLICT`, or `UNKNOWN` results before every write. | State machine / adversarial | PASS | Hydration and public-state fixtures preserve the complete frozen tuple, admit no public call before exact `FINAL`, and exercise all five closed classifications before each write. |
| T-07 | AC-07 — OIDC-only npm then exact tag then Release | Inspect workflow bytes and injected client traces for one exact dispatch and one real-checkout directory publish, followed only after canonical npm proof by one merge-SHA tag and then one deterministic asset-free published Release. | Publication / control | PASS | Workflow guards and injected traces prove one OIDC dispatch and one real-checkout publish, followed only after exact proof by one lightweight merge-SHA tag and one deterministic asset-free Release. |
| T-08 | AC-08 — resume skips completed stages and each mutator writes once | Cover all absent, all exact, active publication, each partial-success boundary, lost response, and fresh reconstructed invocation; assert first-safe-stage selection and immutable per-stage call counts across STANDARD, workflow, npm, tag, and Release actions. | Resume / idempotency | PASS | Multi-invocation fixtures cover all-absent, all-exact, active, partial-success, and lost-response states; each reconstruction skips exact stages and calls each eligible mutator at most once. |
| T-09 | AC-09 — ambiguity and failure never grant another mutation | Inject failure, cancellation, timeout, malformed/partial responses, delayed consistency, concurrency drift, and runner/API errors at every stage; assert bounded reads, exact blocking stage/recovery, and zero retry, rerun, fallback, force, edit, delete, repair, or later write. | Failure / safety | PASS | Hostile adapter/public-core fixtures return bounded fail-closed blocks for every failure and ambiguity class, with read-only reconciliation and no retry, fallback, destructive repair, or later write. |
| T-10 | AC-10 — only fresh canonical proof reaches COMPLETE | Make commands, dispatch, CI, workflow text, cached metadata, or logs claim success while exact npm/run/tag/Release or merge proof is missing or wrong, then provide one exact final snapshot; only the latter may complete. | Final evidence / adversarial | PASS | Cache-bypass, tarball, signature/provenance, attempt, tag, Release, and merge fixtures reject command/log claims and allow `COMPLETE` only from one fresh exact canonical snapshot. |
| T-11 | AC-11 — secret and log data is bounded and redacted | Inject token, JWT, OTP, auth URL, cookie, Authorization header, credential environment, API-body, and oversized-log shapes through success and failure; inspect every output and retained structure for deterministic recursive redaction and bounds. | Security / privacy | PASS | Command-cache and public diagnostic fixtures recursively redact every credential shape and bound external bodies/logs on success and failure paths. |
| T-12 | AC-12 — history and external configuration remain unchanged | Compare Task 0085 and delivered pair paths/modes/bytes plus repository/package visibility, private/access/registry fields, workflow permissions/environment identity, rules/protection reads, dependencies, tags, Releases, and public state before and after implementation. | Preservation / compatibility | PASS | Task 0085 blobs/modes and all 169 historical pair paths are unchanged; manifest/workflow assertions and read-only snapshots preserve configured visibility, registry/access, permissions, dependencies, tags, Releases, and public state. |
| T-13 | AC-13 — all owners and projections use one shared graph and command | Inspect imports, package/direct-install inventories, Skill/reference links, docs/prompts/templates, parser/dispatcher actions, validators, planner routing, and dependency graph for one user route, one adapter/core, one checkpoint, six Skills, no production dependency, and no alternate ledger. | Architecture / documentation | PASS | Foundation, instruction, distribution, installation, DAG, and planner suites prove one plain route, one adapter/core and checkpoint, six Skills, zero production dependencies, and synchronized owners/projections. |
| T-14 | AC-14 — complete mock-only verification passes with zero live writes | Run focused and behavioral suites, inspect all external call traces, run the Release planner/composite and four stable commands, validate the final pair, and close final-diff coverage while prohibiting the live deliver route. | Stable / Release / coverage | PASS | Original focused commands passed 230/236 and 112/112, the correction-focused command passed 212/216 with four expected environment skips, six behavioral fixtures passed, Release planning selected `npm run release:ci`, the 490-test composite and all four standalone stable checks passed, and no live target was called. |
| T-15 | AC-15 — terminal handoff exposes only the later plain release action | Inspect implementation terminal output, Skill guidance, dispatcher messages, README/prompts, and Task/Test handoff; require exactly one next `$kyw-deliver 0086` action and no suffix suggestion or implementation-time public call. | Handoff / authority | PASS | Final instruction and scope review expose exactly `$kyw-deliver 0086` as the next action; the retired suffix is unsupported and implementation issued no live dispatch, publish, tag, Release, or ref mutation. |
| T-16 | AC-16 — prepared continuity is applied inside one adapter execution without payload exposure | Inject prepared/no-preparation/failing apply paths around the sole dispatcher; run the same success/failure boundary from the actual npm-packed and installed adapter; assert direct object identity, `hydrate → dispatch → apply`, dispatcher exactly once, apply zero or one time, payload-free result serialization, unchanged checkpoint bytes on failure, and no downstream mutation trace. | Adapter / packaged integration / failure | PASS | Unit traces prove prepared `1/1`, no-preparation `1/0`, and mismatched selection `1/0`; failed apply exposes no result, preserves exact checkpoint bytes, and leaves the seven-stage downstream trace empty. The actual npm-pack/install path proves success and no-preparation plus the default installed core's malformed prepared-state failure, while serialized results and installed instructions expose no token, checkpoint, or manual command; existing core regressions retain queue, ancestry, immutable-pair, CAS/locking, idempotence, and atomicity proof. |

## Regression Coverage

- Task 0085's frozen tuple, `STANDARD`-first gate, exact-head/synthetic/post-main roles, OIDC-only publication, exact-SHA tag/Release, canonical proof, monotonic resume, completed-stage skip, one-write limits, redaction, and no-retry/fallback/force/delete guarantees remain intact.
- Contract-3 and contract-4 delivered-pair immutability, contract 1–3 readers, hard dependencies, one-active selection, reasoned `NONE`, continuity causal lag/no-self-coverage, and checkpoint atomicity remain compatible without historical rewrites or Task-ID exceptions.
- Versionless contract 1–3 `STANDARD` pairs retain GitHub-only delivery/report semantics and never infer a manifest version or enter npm/tag/Release; GitHub exact-SHA remains the continuity and dependency-satisfaction ledger after public `latest` advances.
- `$kyw-impl` and its three managed aliases remain implementation-only and stop at `DONE/PASSED`; `$kyw-task` remains author-only; `$kyw-audit` remains independent; the visible Skill inventory remains exactly six.
- Public npm `0.1.0` through `0.1.4`, the historical `v0.1.4` tag/Release and their evidence remain historical facts; implementation does not mutate them or falsely claim `0.2.0` public completion.
- Package `private: false`, public `publishConfig` and registry, zero production dependencies, no lifecycle installation, direct/plugin parity, installer ownership/recovery, and package allowlist remain unchanged apart from synchronized version and required contract/runtime bytes.
- Repository visibility, GitHub workflow environment/permissions, Trusted Publisher identity, branch protection/rulesets/reviews, and account configuration remain read-only and unchanged.
- Development tests, candidate generation, CI, and terminalization use fixtures/mocks/owned loopback only and never dispatch the live workflow, publish npm, create a tag, or create/edit a Release.
- No optional dry run, plugin-directory submission, dist-tag operation, Release asset, automatic retry, background continuation, or alternate registry/provider path becomes required or authorized.
- Delivery selection no longer emits a token or requires a manual apply command; direct in-memory apply reuses the existing queue, terminal-pair, ancestry, CAS, locking, idempotence, and atomicity path, and a failed apply cannot release a success result to downstream delivery/public mutation.

## Commands

- `node --test test/kyw-deliver.test.mjs test/task-dispatch.test.mjs test/task-public-release.test.mjs test/task-delivery-hydration.test.mjs test/task-delivery-continuity.test.mjs test/task-artifacts.test.mjs test/task-artifact-prevalidation.test.mjs test/template-contracts.test.mjs test/kyw-task.test.mjs test/kyw-impl.test.mjs test/publish-workflow.test.mjs`
- `node --test test/foundation.test.mjs test/instruction-surfaces.test.mjs test/distribution.test.mjs test/skill-installation.test.mjs test/cli.test.mjs test/npm-publish-git-head.test.mjs test/verification-plan.test.mjs`
- `node --test test/task-delivery-continuity.test.mjs test/task-delivery-hydration.test.mjs test/task-dispatch.test.mjs test/skill-installation.test.mjs test/distribution.test.mjs test/kyw-deliver.test.mjs test/instruction-surfaces.test.mjs`
- `node ./scripts/spec-behavioral-acceptance.mjs --validate-fixtures`
- `npm run verify:plan -- package.json .codex-plugin/plugin.json AGENTS.md CODEX_PROMPTS.md README.md docs/SPEC.md docs/ARCHITECTURE.md templates/project/AGENTS.md templates/task/TASK.md templates/task/TEST.md skills/kyw-deliver/SKILL.md skills/kyw-deliver/agents/openai.yaml skills/kyw-deliver/references/delivery.md skills/kyw-deliver/references/public-release.md skills/kyw-task/SKILL.md skills/kyw-task/scripts/task-artifacts.mjs skills/kyw-impl/SKILL.md skills/kyw-impl/references/execution.md src/core/template-contracts.mjs src/core/package-info.mjs src/cli/run.mjs src/core/task-artifact-creation.mjs src/core/task-artifact-contract.mjs src/core/task-artifact-delivery.mjs src/core/task-artifact-queue.mjs src/core/task-artifact-hydration.mjs src/core/task-artifact-public-release.mjs src/core/task-artifacts.mjs src/core/skill-installation-inventory.mjs scripts/lib/validate-foundation.mjs scripts/verification-plan.mjs test/foundation.test.mjs test/instruction-surfaces.test.mjs test/distribution.test.mjs test/skill-installation.test.mjs test/verification-plan.test.mjs test/kyw-deliver.test.mjs test/task-dispatch.test.mjs test/task-public-release.test.mjs test/task-delivery-hydration.test.mjs test/task-delivery-continuity.test.mjs test/task-artifacts.test.mjs test/task-artifact-prevalidation.test.mjs test/template-contracts.test.mjs test/kyw-task.test.mjs test/kyw-impl.test.mjs test/cli.test.mjs test/npm-publish-git-head.test.mjs test/publish-workflow.test.mjs docs/tasks/0086-unify-standard-delivery-for-kyw-dev-0-2-0/TASK.md docs/tasks/0086-unify-standard-delivery-for-kyw-dev-0-2-0/TEST.md`
- `npm run release:ci`
- `npm test`
- `npm run lint`
- `npm run format:check`
- `npm run pack:check`
- `npm view kyw-dev versions --json --prefer-online --registry=https://registry.npmjs.org/`
- `npm view kyw-dev dist-tags --json --prefer-online --registry=https://registry.npmjs.org/`
- `node --input-type=module -e "const r=await fetch('https://registry.npmjs.org/kyw-dev',{cache:'no-store'}); if(!r.ok) throw new Error('registry read failed'); const p=await r.json(); const versions=Object.keys(p.versions??{}); if(versions.includes('0.2.0')) throw new Error('0.2.0 occupied'); console.log(JSON.stringify({latest:p['dist-tags']?.latest,versions}));"`
- `git ls-remote --refs --tags origin refs/tags/v0.2.0`
- `gh api 'repos/kimyeongwoo/kyw-dev/git/matching-refs/tags/v0.2.0' --jq 'if length == 0 then {tag:"ABSENT"} else error("v0.2.0 occupied") end'`
- `gh api --paginate 'repos/kimyeongwoo/kyw-dev/releases?per_page=100' --jq '.[] | select(.tag_name == "v0.2.0")'`
- `gh api 'repos/kimyeongwoo/kyw-dev/actions/workflows/publish.yml/runs?event=workflow_dispatch&per_page=100' --jq '[.workflow_runs[] | {id,run_attempt,status,conclusion,head_sha,display_title}]'`
- Manual read-only publication-run proof — paginate the `publish.yml` workflow-dispatch run envelope to the configured bound, inspect every authoritative attempt through the existing bounded/redacted log reader, require exactly one canonical `KYWPUBLISHEVIDENCE schema=1 stage=dispatch ... expected_version=<version>` marker per attempt, and block on `expected_version=0.2.0`, missing or duplicate markers, truncation, incomplete pagination, or unreadable evidence; retain only run/attempt/version/classification.
- `node skills/kyw-task/scripts/task-artifacts.mjs validate --task-directory docs/tasks/0086-unify-standard-delivery-for-kyw-dev-0-2-0`
- `node skills/kyw-task/scripts/task-artifacts.mjs inspect-transaction --tasks-root docs/tasks`
- `git diff --check`

## Results

- PASS — read-only baseline: npm reports only `0.1.0`–`0.1.4` with `latest=0.1.4`; direct and GitHub ref reads report `v0.2.0` absent; Release-by-tag and bounded matching publication-run reads are empty.
- PASS — Task 0085 canonical pair entries remain regular mode `100644` at blobs `d0bcd08b1576388c27a6ef994355cf98ab85e456` and `c09c11eb047ce1887cf27286dfee1140216abd1b` before implementation.
- FAIL→PASS — the initial focused command completed 233 tests with 219 passing, 8 failing, and 6 skipped after instruction compaction removed exact safety wording; the exact safety wording was restored before all final reruns.
- PASS — the final focused routing/runtime/public-release/contract/workflow command completed 236 tests with 230 passing, 0 failing, and 6 expected environment skips.
- PASS — the focused foundation/instruction/distribution/installation/CLI/git-head/planner command completed 112/112 tests with zero failures or skips.
- FAIL→PASS — the first correction-focused rerun exposed three stale instruction/document assertions; after exact owner wording was corrected, the aggregate reached 216 tests with 211 passing, 1 failing, and 4 expected environment skips because the new T-16 row used unsupported intermediate status `PENDING`. Changing only that row to valid `TODO` restored validation before terminalization.
- PASS — the final correction-focused command completed 216 tests with 212 passing, 0 failing, and 4 expected environment skips; focused success/no-preparation/failure adapter checks also passed 3/3 through the actual packaged-install path.
- PASS — direct behavioral acceptance returned `valid: true`, method `CURRENT_SESSION_DIRECT`, for all 6 deterministic scenarios.
- PASS — the exact changed-path planner selected `RELEASE` and the single composite command `npm run release:ci`; hosted exact-SHA CI remains a delivery-time gate and was not claimed as implementation evidence.
- PASS — `npm run release:ci` completed 490 tests with 486 passing, 0 failing, and 4 expected environment skips; lint passed 85 JavaScript modules, format passed 384 files, pack passed 48 files / 200,256 bytes, and the candidate SHA-256 was `235d882dd97972441db4aa0bfbb366c64cef28d42368331d3767bbb312e33073`.
- PASS — the standalone stable commands reproduced the result: `npm test` completed 490 tests with 486 passing, 0 failing, and 4 expected environment skips; lint passed 85 modules, format passed 384 files, and pack passed 48 files / 200,256 bytes.
- PASS — fresh read-only public proof at `2026-09-04T09:45:19Z` reports npm versions `0.1.0`–`0.1.4` with `latest=0.1.4`, while npm `0.2.0`, `v0.2.0`, and its GitHub Release are absent; all three publication attempts remain exact historical `0.1.2`–`0.1.4` runs, so no conflicting `0.2.0` attempt exists.
- PASS — Task 0085 remains mode `100644` at blobs `d0bcd08b1576388c27a6ef994355cf98ab85e456` / `c09c11eb047ce1887cf27286dfee1140216abd1b`, and all 169 historical Task/Test paths are unchanged.
- PASS — fixture, mock, owned-loopback, candidate, and verification execution issued no live workflow dispatch, npm publication, tag creation, GitHub Release creation, or ref mutation.
- PASS — the terminal contract-4 pair validates with release version `0.2.0`; transaction inspection reports `NONE` / `NO_TRANSACTION_EVIDENCE`, and `git diff --check` passes.

<!-- kyw-permanent-document-delta:v1 -->

| Path | Before bytes | After bytes | Before lines | After lines | Byte delta | Percent | Canonical owner | Durable necessity | Replacement or absorption |
|---|---:|---:|---:|---:|---:|---:|---|---|---|
| `README.md` | 18421 | 19129 | 225 | 226 | +708 | 3.84% | setup, installation, commands, usage, and contributor entry | Users need the sole plain command, same-invocation result, selected source version, mutable-registry distinction, and state-safe installation command. | Existing routing, release-status, and installation sections replace two-route, pinned-version, and fixed-latest wording without a new owner. |
| `AGENTS.md` | 4094 | 4093 | 41 | 41 | -1 | -0.02% | repository-wide Codex routing, authority, preservation, and completion rules | Managed repositories need the contract-4 continuation and Task-owned version boundary at routing time. | Existing routing and evidence bullets absorb the new contract while keeping the file within its 4 KiB warning budget. |
| `docs/SPEC.md` | 48996 | 48785 | 479 | 482 | -211 | -0.43% | observable product behavior, safety, publication authority, and acceptance | Product truth must define one plain route, prospective release versions, historical compatibility, source-versus-registry truth, and fail-closed automatic continuity application. | Existing Task, delivery, package, publication, and acceptance sections replace obsolete two-route, manual-apply, and fixed-latest meaning and remain smaller overall. |
| `docs/ARCHITECTURE.md` | 48298 | 50344 | 935 | 966 | +2046 | 4.24% | components, boundaries, dependencies, control and data flow, and distribution | Architecture must locate contract-4 version flow and the same-invocation continuity/public handoff inside the existing adapter, hydration, public core, and canonical ledgers. | Existing route, delivery, artifact, continuity, and publication sections absorb the new flow; detailed mechanics stay in the two progressively loaded references. |
| `Combined` | 119809 | 122351 | 1680 | 1715 | +2542 | 2.12% | four permanent documents as one governed set | The governed set must consistently expose one delivery authority, direct in-memory continuity handoff, version ownership, compatibility, and mutable public-state boundary. | All meaning remains in the four established owners with no fifth document, copied engine, alternate ledger, or budget increase. |

## Unverified

- The live `kyw-dev@0.2.0` publication workflow, npm mutation, tag creation, and GitHub Release creation remain intentionally unexecuted; only the later exact plain `$kyw-deliver 0086` invocation may attempt them.
- Live credential availability, GitHub/npm authorization, API latency, and eventual consistency are not proven by fixtures; the production route must classify unavailable or ambiguous evidence as `BLOCKED` without configuration changes, fallback, or retry.
- Registry, workflow, tag, Release, branch, and hosted-CI state can change after local verification. Delivery must freeze and freshly recheck every public surface and satisfy hosted exact-SHA CI gates before each eligible write.

## Final Coverage Review

- [x] Compare the final diff to the matrix.
- [x] Map every acceptance criterion to one or more test rows.
- [x] Add coverage for introduced branches, failures, and compatibility behavior.
- [x] Confirm PASS evidence is reproducible.
- [x] Confirm required regressions ran.
