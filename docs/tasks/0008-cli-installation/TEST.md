# TEST 0008 — CLI Installation

## Status

PASSED

## Test Basis

- Task: `./TASK.md`
- Product requirements: `../../SPEC.md`
- Architecture constraints: `../../ARCHITECTURE.md`

## Intent-to-Test Matrix

| ID | Intent / acceptance criterion | Method | Level | Status | Evidence |
|---|---|---|---|---|---|
| T-01 | AC-01 user scope installs complete Skills | Run with isolated HOME | Integration | PASS | Actual CLI entrypoint installed four Skills into an isolated HOME, doctor found the managed 0.1.0 copy, and CLI uninstall removed it cleanly. |
| T-02 | AC-02 project scope installs in Git root | Run in nested fixture repo | Integration | PASS | Actual CLI entrypoint started in `packages/api`, resolved the fixture Git root, installed only root `.agents/skills`, and preserved/created no project documents. |
| T-03 | AC-03 unmanaged/modified conflict is preserved | Run overwrite-negative fixtures | Integration | PASS | Unmanaged directory and real-junction installs returned conflict without metadata or target mutation; locally modified update preserved the complete pre-call snapshot. |
| T-04 | AC-04 update behavior respects hashes | Run unchanged and modified update cases | Integration | PASS | Clean 0.1.0 files advanced to a 0.2.0 source inventory and hashes; a modified installed file produced `UPDATE_CONFLICT` with bytes and metadata unchanged. |
| T-05 | AC-05 uninstall preserves unrelated files | Run mixed Skills fixture | Integration | PASS | Default uninstall refused modified/unknown state; forced uninstall removed modified owned files but preserved the unknown nested file and unrelated Skill. |
| T-06 | AC-06 doctor diagnoses runtime/tool availability, version drift, duplicates, malformed package/Skill metadata, permissions, and partial state without mutating either scope | Run diagnostic fixtures and before/after inventories | Integration | PASS | Doctor reported duplicate scopes, malformed Skill/plugin, permission failure, Codex absence, unsupported Node, version drift, and partial transactions; user/project snapshots remained identical. |
| T-07 | AC-07 interrupted update is recoverable | Inject stage/swap failure | Integration | PASS | Child-process exits after stage and first swap were diagnosed; recovery discarded the first stage and rolled the second back to every prior metadata/file hash before update retry passed. |
| T-08 | AC-08 cross-platform paths | Run path unit matrix | Unit | PASS | POSIX and Windows user/project layouts and parent walks passed on Windows; nested real repository resolution preserved cwd. |
| T-09 | AC-01, AC-02, and AC-09 metadata and direct-install runtime support are complete | Inspect schema/hash inventory, then execute the installed `kyw-task` adapter against an isolated repository | Integration / packaging | PASS | Schema 1 recorded 19 sorted hashes and four Skill paths; all installed hashes matched, and the direct-installed adapter created an atomic `0001` Task/Test pair through hidden runtime support. |
| T-10 | AC-10 command grammar, help/version mutation boundary, and stable exit categories are deterministic | Run valid/invalid CLI tables and injected error categories | Unit / integration | PASS | Existing binary help/version mutation tests and new grammar/category tables passed exits 0 through 7, including usage, runtime, scope, conflict, invalid state, permission, and orphan recovery cases. |
| T-11 | AC-01 through AC-10 preserve package contents, zero dependencies, no lifecycle scripts, existing Skill contracts, and stable repository checks | Run full stable, Node.js 22, coverage, direct pack, and Task validation checks | Regression / packaging | PASS | Stable and Node.js 22 suites passed 70/70; coverage, official Skill/Task validation, exact 29-entry dry-run package, actual packed lifecycle smoke, and snapshot scope audit passed. |

Every acceptance criterion must reference one or more rows before the Task becomes `READY`. Add rows for meaningful behaviors discovered in the final diff.

## Regression Coverage

- [x] Unrelated user/project Skills are preserved.
- [x] Project documents are never generated by install/update.
- [x] Help/version remain read-only and fast.
- [x] Direct-install support stays namespaced under `.agents/skills/.kyw-dev/` and matches canonical packaged source hashes.
- [x] Package remains dependency-free and lifecycle-script-free.

## Commands

Planned commands; replace or extend them with the commands the repository actually supports.

- `npm test -- --test-name-pattern="install|update|uninstall|doctor|path"`
- `npm test`
- `npm run lint`
- `npm run format:check`
- `npm run pack:check`
- `npx --yes node@22 --test`
- `node --test --experimental-test-coverage`
- `npm pack --dry-run --json`
- `node skills/kyw-task/scripts/task-artifacts.mjs validate --task-directory docs/tasks/0008-cli-installation`
- `git -c core.autocrlf=false diff --no-index --no-renames` name/status, stat, content, and `--check` against the verified pre-change snapshot.
- PowerShell file-set and SHA-256 comparison against the verified pre-change snapshot.

## Results

Record exact commands, exit status, and concise outcome. Do not mark a row PASS without evidence.

- Pre-change `git status --short --branch`, `git diff --stat`, and staged-diff inspection: exit 128, `fatal: not a git repository`; the workspace contains no `.git` metadata.
- Verified pre-change snapshot: 95 source files and 95 snapshot files, zero missing paths, and zero SHA-256 mismatches. `git diff --no-index --quiet` exited 0 between the snapshot and workspace.
- Pre-change `npm run check`: exit 0; 52/52 tests passed, lint checked 18 JavaScript modules plus foundation metadata, format checked 87 UTF-8/LF text files, and pack check matched 28 files at 32,034 bytes. This is baseline evidence only.
- Initial focused install/update/uninstall/doctor/path run: exit 1; 19/20 selected checks passed. The sole failure used default Unicode sorting to verify an inventory deliberately ordered with `localeCompare`; the assertion was aligned with the production comparator.
- Corrected focused run: exit 0; 20/20 selected checks passed, including isolated user/project installs, direct-installed adapter execution, conflicts, update, force uninstall, doctor, staged/swap recovery, exit categories, and path dialects.
- Preliminary post-implementation `npm run check`: exit 0; 65/65 tests passed, lint checked 20 JavaScript modules plus foundation metadata, format checked 89 UTF-8/LF text files, and pack check matched 29 files at 44,898 bytes.
- Built-in coverage before final scenario/doc refinements: exit 0; 65/65 tests passed. `skill-installation.mjs` reached 80.34% lines, 65.63% branches, and 97.85% functions; aggregate runtime coverage was 83.98% lines, 69.50% branches, and 98.09% functions.
- First full run after permanent-document synchronization: exit 1; 66/67 tests passed. The only failure was the prior `kyw-task` regression assertion fixed to README status “Tasks 0001 through 0007”; it was advanced to the intentional Task 0008 text.
- Corrected preliminary stable commands: all exited 0. Full tests passed 67/67; lint checked 20 modules plus metadata; format checked 89 files; pack check matched 29 files at 45,819 bytes.
- The first standalone actual-tarball smoke attempt was rejected before execution by the environment because its PowerShell `Remove-Item -Recurse` cleanup was disallowed. It produced no repository or temporary artifact. The same scenario was added as an automated Temp-scoped test using the existing verified cleanup helper.
- Automated actual-tarball smoke: exit 0. `npm pack` produced 29 entries; the extracted CLI installed into an isolated HOME, doctor returned healthy with empty stderr, the installed adapter created `0001-packed-smoke/{TASK,TEST}.md`, uninstall removed ownership metadata, and the test cleanup removed the temporary tree.
- Final focused command: exit 0; 25/25 selected checks passed, including real binary user/project installation, paths, conflicts, update/uninstall, all doctor categories, staged/swap recovery, live-owner refusal, exit codes, source inventory, and actual tarball lifecycle.
- Final pre-terminal `npm run check`: exit 0; 70/70 tests passed, lint checked 20 JavaScript modules plus foundation metadata, format checked 89 UTF-8/LF files, and pack check matched 29 files at 45,805 bytes.
- Final pre-terminal `npx --yes node@22 --test`: exit 0; 70/70 tests passed with no skips on the minimum supported runtime. A preceding run exposed a Windows `shell: true` deprecation warning; after the command-runner correction, targeted Node.js 22 packed/doctor tests passed with empty stderr.
- Final pre-terminal built-in coverage: exit 0; 70/70 tests passed. `skill-installation.mjs` reached 83.60% lines, 69.53% branches, and 100% functions; aggregate coverage was 86.30% lines, 72.50% branches, and 99.37% functions.
- Final direct `npm pack --dry-run --json`: exit 0; `kyw-dev@0.1.0` contained exactly 29 entries at 45,911 packed and 166,760 unpacked bytes, including the installation core and all existing plugin/Skill/template/legal files, with no development test or Task files.
- Official `quick_validate.py skills/kyw-task` and direct Task 0008 validation both exited 0.
- Pre-terminal no-index review found exactly 10 expected modified files and 2 expected additions. Stat/content review and `--check` produced no whitespace diagnostic; the independent audit found 95 before files, 97 after files, 0 deletions, 85 byte-identical existing files, no expected-set mismatch, and no root tarball.
- First post-terminal direct coverage rerun: exit 1; 69/70 passed. The actual-tarball test's Windows fallback passed a quoted command string through `cmd /s /c`, causing npm to treat the quote characters as part of the destination. This branch is used only when `npm_execpath` is absent, which is why the preceding `npm run` and `npx` suites passed. The invocation was corrected to pass separate `cmd /c` arguments and requires a full rerun before restoring `PASSED`.
- Corrected direct tarball test: exit 0; 1/1 passed without `npm_execpath`. Corrected direct coverage: exit 0; 70/70 passed; `skill-installation.mjs` reached 83.49% lines, 69.02% branches, and 100% functions, with 86.17% / 72.08% / 99.37% aggregate coverage.
- Post-fix `npm run check`: exit 0; 70/70 tests passed, lint checked 20 JavaScript modules plus foundation metadata, format checked 89 UTF-8/LF files, and pack check matched 29 files at 45,911 bytes.
- Post-fix `npx --yes node@22 --test`: exit 0; 70/70 tests passed with no skips, warnings, or stderr diagnostics.
- Post-fix standalone `npm pack --dry-run --json`: exit 0; exactly 29 entries at 45,911 packed and 166,760 unpacked bytes, with no Task or test development files. Official Skill and Task validators both exited 0.
- Post-fix no-index/hash audit: 95 before files and 97 after files; exactly 10 expected modifications, 2 expected additions, 0 deletions, and 85 byte-identical existing files. Expected-set mismatches, root tarballs, and whitespace diagnostics were all empty. The no-index commands exited 1 solely because the expected content differences exist.

## Unverified

- Git working-tree, staging, and history state cannot be verified because the supplied workspace has no `.git` metadata. Snapshot-based review can establish Task 0008 content changes but cannot recover Git state.
- Native filesystem integration ran on Windows. POSIX and Windows path dialects are covered deterministically, and Node.js 22 runs the full suite, but native macOS/Linux permission and process-interruption semantics remain a release-level portability risk.

## Final Coverage Review

Before marking this Test `PASSED`:

- [x] Compare the final diff to the matrix.
- [x] Map every acceptance criterion to at least one test row.
- [x] Add tests for newly introduced branches, error paths, or compatibility behavior.
- [x] Confirm recorded PASS claims are reproducible.
- [x] Confirm required regressions ran.

Coverage mapping is complete: AC-01 -> T-01/T-09/T-11; AC-02 -> T-02/T-09/T-11; AC-03 -> T-03/T-10; AC-04 -> T-04/T-07; AC-05 -> T-05/T-07; AC-06 -> T-06/T-10; AC-07 -> T-07; AC-08 -> T-08; AC-09 -> T-09/T-11; AC-10 -> T-10/T-11. CLI/core/adapter/package behavior maps to T-01 through T-11; permanent-document and prior-workflow changes map to T-10/T-11; every final fixture, failure, recovery, and compatibility branch is covered by its acceptance row.
