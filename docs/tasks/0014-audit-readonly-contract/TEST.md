# TEST 0014 — Audit Read-Only Contract

## Status

PASSED

## Test Basis

- Task: `./TASK.md`
- Product requirements: `../../SPEC.md`
- Architecture constraints: `../../ARCHITECTURE.md`

## Intent-to-Test Matrix

| ID | Intent / acceptance criterion | Method | Level | Status | Evidence |
|---|---|---|---|---|---|
| T-01 | AC-01 invocation contract is unambiguous | Inspect canonical Skill, metadata, Spec, and README | Contract/Audit | PASS | Contract tests prove bare read-only, exact `--fix`, and natural-language redirect across canonical/public surfaces. |
| T-02 | AC-02 default audit writes nothing | Hash tracked/untracked fixture tree before and after | E2E/Security | PASS | Fresh read-only run: identical tree SHA-256 `769990ad...87428ee7`, identical Git status, zero file-change/mutating-command events. |
| T-03 | AC-03 read-only findings remain complete | Run fixture with scope/docs/test failures and inspect report | Behavior/E2E | PASS | Fresh inconsistent fixture produced a stable `F-` finding and final `BLOCKED`; static report assertions cover evidence, drift, residual risk, and structured verdict fields. |
| T-04 | AC-04 repair requires the exact flag and a pre-mutation plan | Bare/natural-language negative invocation tests plus `--fix` transcript ordering | Behavior | PASS | Deterministic mode negatives passed; fresh fix event stream recorded `planBeforeMutation: true` with the standalone `Bounded repair plan:` contract. |
| T-05 | AC-05 repairs remain in scope | Fixture with clear in-scope and tempting out-of-scope issues | E2E | PASS | Fresh fix changed exactly Task, Test, source, and source-test paths; pre-existing tracked/untracked/generated bytes and status entries were preserved. |
| T-06 | AC-06 repaired checks are rerun and limitations reported | Inspect commands/evidence after fix mode | E2E/Audit | PASS | Harness directly reran the repaired fixture test successfully and required the final report's finding plus `PASS` signals; Skill gates retain failures/limitations. |
| T-07 | AC-07 deterministic and fresh-session checks pass | Run static tests and model-backed smoke scenarios | Unit/Eval | PASS | Focused tests 14/14; final fresh `gpt-5.6-sol`/high read-only and fix sessions both passed the evaluator. |
| T-08 | AC-08 every packaged copy agrees | Search package source and inspect packed tarball | Packaging/Audit | PASS | Distribution test compares all three installed audit files byte-for-byte; pack check and 29-file npm dry run passed. |

Every acceptance criterion must reference one or more rows before the Task becomes `READY`. Add rows for meaningful behaviors discovered in the final diff.

## Regression Coverage

- [x] Bare `$kyw-audit <ID>` changes no file and runs no mutating command.
- [x] Natural-language repair intent changes no file and directs the user to the exact `--fix` invocation.
- [x] Read-only audit still returns useful stable findings and one final verdict.
- [x] `--fix` preserves unrelated modified and untracked files.
- [x] `--fix` does not allocate a follow-up Task or broaden product scope.
- [x] Required tests rerun after a repair.
- [x] All Skill packaging/install tests remain green.

## Commands

- `node --test test/kyw-audit.test.mjs test/audit-smoke.test.mjs`
- `node --check scripts/audit-smoke.mjs`
- `python C:/Users/DevHamster/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/kyw-audit`
- `node ./scripts/audit-smoke.mjs --allow-model --mode readonly --model gpt-5.6-sol --reasoning-effort high --auth-file <explicit-auth-source>`
- `node ./scripts/audit-smoke.mjs --allow-model --mode fix --model gpt-5.6-sol --reasoning-effort high --auth-file <explicit-auth-source>`
- `node --test test/foundation.test.mjs`
- `npm test`
- `npm run lint`
- `npm run format:check`
- `npm run pack:check`
- `npm run check`
- `npm pack --dry-run --json`
- `node skills/kyw-task/scripts/task-artifacts.mjs validate --task-directory docs/tasks/0014-audit-readonly-contract`
- `git diff --check`
- `git status --short`

## Results

- Focused deterministic checks: exit 0; 14/14 tests passed. Syntax check and official Skill validation also passed.
- Fresh read-only behavior: exit 0 under `codex-cli 0.144.5`; model `gpt-5.6-sol`, effort `high`, installed Skill source-read proof true, authentication source unchanged, mutation attempts `0`, verdict `BLOCKED`. Tree before/after was exactly `769990ad4fad1fde26183ae45454e9ead6b33be9d7fa822261a2d30d87428ee7`. Git status before/after was exactly:

  ```text
   M notes/user-draft.md
  ?? generated/cache.txt
  ?? scratch/idea.txt
  ```

  Final-message SHA-256: `6e2b0a83f0cf90d73f68bb98c4b0ec510c3f819de6038f27cf8105173bdd7c3a`.
- Fresh fix behavior: exit 0 under the same CLI/model/effort; source-read proof and authentication preservation true; bounded plan preceded the first mutation; verdict `PASS`. Tree changed from `769990ad4fad1fde26183ae45454e9ead6b33be9d7fa822261a2d30d87428ee7` to `ab6b6ebc6cac85f4b9eced10de5ea491e84f411eb1876dad66e5fd65efc22799`. Changed paths were exactly:

  ```text
  docs/tasks/0001-greeting-contract/TASK.md
  docs/tasks/0001-greeting-contract/TEST.md
  src/greeting.mjs
  test/greeting.test.mjs
  ```

  The three unrelated dirty entries remained present and byte-identical; the harness reran the fixed fixture test successfully. Final-message SHA-256: `face020e612616befc63e71b3675d3a61a2fc7c2d9da1573ed17b6818805cdd0`.
- Retained evaluator discoveries before the final passes: generic `gpt-5.6` was unsupported for the authenticated ChatGPT surface; two initial fix runs inherited effective read-only behavior; a clean-home attempt reached the command router but shell execution was denied; the first outer-sandbox attempt failed TLS trust; and the next fix run exposed that the repair plan was not a separately observable pre-mutation message. The runner now uses the exact supported `gpt-5.6-sol`, an evaluator-owned outer permission profile plus temporary CA bundle, and the canonical standalone `Bounded repair plan:` rule.
- The first final `npm test` rerun exposed one stale foundation phrase and failed 118/119. After synchronizing that assertion, `node --test test/foundation.test.mjs` passed 1/1 and `npm test` passed 119/119. The subsequent aggregate `npm run check` again passed 119/119 plus lint, format, and pack gates.
- Individual stable commands passed: lint covered 33 JavaScript modules/foundation metadata; format covered 153 UTF-8/LF files; pack check covered 29 files and reported 58,990 bytes.
- `npm pack --dry-run --json`: exit 0; 29 entries, package size 58,990 bytes, unpacked size 228,652 bytes; development-only smoke sources and fixtures were absent from the tarball.
- Task artifact validation, `git diff --check`, final status inspection, and path-scoped diff review passed. The status inspection retained pre-existing Task 0011–0013/future-Task, grilling/eval, release, and filesystem-hardening changes; Task 0014 did not clean, stage, commit, publish, or absorb them.

## Unverified

- The model-backed smoke was executed on Windows with Codex CLI `0.144.5` and current `gpt-5.6-sol`. Static/deterministic tests cover the contract elsewhere, but native-sandbox CLI behavior and model output can vary on other OS/CLI/model revisions; rerun both explicit smoke modes when those inputs change.

## Final Coverage Review

Before marking this Test `PASSED`:

- [x] Confirm the no-write claim includes tracked, untracked, generated, Task/Test, and durable-doc state.
- [x] Confirm every repair diff hunk maps to an audited in-scope finding.
- [x] Confirm out-of-scope findings remain proposals only.
- [x] Confirm canonical and distributed Skill text cannot drift on the contract.
- [x] Confirm user-facing examples use the final chosen syntax.
