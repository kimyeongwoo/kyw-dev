# TEST 0047 — Post-Hardening Release-Readiness Re-Gate

<!-- kyw-task-contract: 2 -->

## Status

PASSED

## Test Basis

- Task: `./TASK.md`.
- Hard dependency: Task 0046 and its complete prerequisite chain must be repository-complete and externally delivered before implementation.
- Product requirements: `../../SPEC.md`, especially release tiers, distribution, MVP acceptance, publication decisions, evidence honesty, and separate publication approval.
- Architecture constraints: `../../ARCHITECTURE.md`, especially immutable candidate identity, release/CI/isolation boundaries, package contents, protected state, and external delivery.
- Repository rules: `../../../AGENTS.md`.
- Verification policy: use the current repository-defined Release tier; bind every result to exact bytes and provenance; never infer approval or PASS across a missing boundary.

## Model Provenance

- Model identifier: `UNAVAILABLE` (`UNAVAILABLE`: the active API surface does not expose an exact configured identifier)
- Requested model alias: `NOT_REQUESTED` (`OBSERVED`: the current user requested no model override)
- Reasoning effort: `UNAVAILABLE` (`UNAVAILABLE`: the active API surface does not expose the configured effort)
- Codex surface: `API` (`OBSERVED`: the active system identifies access through an API)
- Codex version: `UNAVAILABLE` (`UNAVAILABLE`: the active API surface does not expose a Codex version)

## Intent-to-Test Matrix

| ID | Intent / acceptance criterion | Method | Level | Status | Evidence |
|---|---|---|---|---|---|
| T-01 | AC-01 — Clean delivered exact-main candidate base | Prove local HEAD, direct remote `main`, clean status, hard dependencies, and required external delivery identities before packing. | Integrity/external read-only | PASS | Local/upstream/origin/direct `main` all equal `515a5ff5...`; Task 0046 exact ledger is satisfied; no staged/unstaged path, branch/PR/transaction residue, tag, or Release. |
| T-02 | AC-02 — Current-byte re-gate | Inventory every currently required gate and rerun it for this candidate or record an explicit blocking limitation; do not import Task 0029/0038 PASS. | Audit/release | PASS | Current bytes independently passed the composite Stable/candidate/dry-run gate and deterministic isolation gate; the original registry probes produced the retained name/auth observations, and only the separately reauthorized failed auth boundary was rechecked after a byte-identical preflight. Task 0029/0038 results were not reused as current PASS. |
| T-03 | AC-03 — Evidence and provenance completeness | For every claimed result, record exact command/procedure, exit status, runtime/tool/model observability, candidate SHA, and reproducible output or evidence location. | Audit/provenance | PASS | Exact command counts, exit codes, runtimes, npm/Codex/model provenance, candidate/tarball identities, protected-state comparison, retries, and evidence location are recorded below. |
| T-04 | AC-04 — Fail-closed unavailable boundaries | Exercise or encounter unavailable registry/auth/model/hosted/isolation requirements and require a blocker rather than a guessed PASS. | Failure/audit | PASS | Earlier `AUTH_REJECTED` evidence correctly blocked the pair. After user repair, the separately authorized one-shot `npm whoami` exited `0` with sanitized result `AUTHENTICATED`, while the new post-login userconfig baseline remained byte- and metadata-identical; no unavailable required local boundary remains. |
| T-05 | AC-05 — Actual tarball identity and lifecycle | Verify real archive SHA, allowlist/count, metadata, extracted license/notices, packed CLI/Skills, direct install, plugin boundary, and protected-state lifecycle. | Package/E2E | PASS | Composite candidate and isolation gates agreed on 39 files, 92,971 packed bytes, archive SHA-256 `66afce...ea6`; legal/package/runtime, direct lifecycle, plugin marketplace lifecycle, preservation, and cleanup checks passed. |
| T-06 | AC-06 — Forbidden mutation absence | Inspect commands, Git diff/status, version/tags/releases, registry/public state, and Task inventory for zero publish, mutation, version bump, tag, Release, submission, fix, or Task 0048. | Integrity/security | PASS | Before/after checks show unchanged exact main/package/userconfig/tags/Releases, no actual publication or registry mutation, no version/Task change, and only the expected Task 0047 pair untracked. |
| T-07 | AC-07 — Verdict and delivery consistency | Cross-check Task/Test terminal state, exact-candidate readiness verdict, PR head, merge identity, and post-merge `main` CI. | Audit/external | PASS | Terminal pair state and exact-candidate verdict agree on `DONE/PASSED` plus `READY_FOR_APPROVAL`. Mutable PR head, merge, and post-merge identities remain the separately evaluated `STANDARD` ledger and are deliberately not pre-claimed in repository-local evidence. |
| T-08 | AC-03, AC-04, AC-06 — Separate approval gates | Before any release-check, publish dry run, registry/auth probe, or model-backed marketplace command, verify explicit Task-0047 approval, exact command, non-mutation basis, cost/provenance, and stop rule. | Approval/manual | PASS | Earlier composite/registry/isolation approvals remain bounded. The user separately approved exactly one fresh `npm whoami` with zero retry/model calls and explicitly prohibited every repeated release/name/isolation/model gate plus publication and registry mutation. |

## Regression Coverage

- Stable suite, real candidate archive, package hygiene/legal identity, packed CLI/Skills, direct-install and plugin-cache boundaries, and isolation/protected-state outcomes.
- Exact candidate SHA versus working tree, PR head, merge result, remote `main`, post-merge CI, and external `STANDARD` delivery identity.
- Historical readiness supersession, unavailable-evidence blocking, model/runtime/auth/network provenance, and no duplicate identity proof.
- Absolute prohibition on publish, registry mutation, version bump, tag, GitHub Release, public submission, silent product fixes, automatic Task creation, and inferred approval.

## Commands

- Executed read-only preflight: `git status --porcelain=v2 --branch`, `git rev-parse HEAD`, `git rev-parse @{upstream}`, `git ls-remote origin refs/heads/main`, local/remote branch inventory, PR/Actions inspection, tag/Release inspection, and external-ledger validation through exact `taskId: "0046"`.
- Executed provenance/help: `git --version`, `node --version`, `npm --version`, `codex --version`, `codex exec --help`, `codex plugin --help`, and `codex plugin marketplace --help`.
- Executed current-pair/transaction checks: `node ./skills/kyw-task/scripts/task-artifacts.mjs validate --task-directory docs/tasks/0047-post-hardening-release-readiness-re-gate`; `node ./skills/kyw-task/scripts/task-artifacts.mjs inspect-transaction --tasks-root docs/tasks`.
- Executed Release-tier selection: `npm run verify:plan -- --candidate package.json .codex-plugin/plugin.json src skills templates README.md LICENSE THIRD_PARTY_NOTICES.md licenses`.
- A first pre-execution setup helper exited `1` at PowerShell argument binding because `New-Item` does not support `-LiteralPath`; it launched no npm/release command, created no temporary root, and consumed none of the approved invocation counts. The setup-only syntax was corrected without changing repository or normal user state.
- Executed exactly once: `npm run release:check`. Start `2026-07-25T07:06:31.3277368Z`; end `2026-07-25T07:07:23.2193451Z`; runtime `51,889 ms`; exit `0`. It ran with empty repository-external Task-owned user/global npm configs, new Task-owned cache/log/temp paths, registry `https://registry.npmjs.org/`, and child-only removal of targeted auth environment variables. The package-owned expansion was exactly `npm run release:ci && npm publish --dry-run --json`.
- Did not execute standalone: `npm publish --dry-run --json`. The one dry run was owned by the single composite invocation.
- Executed each exactly once with `--registry=https://registry.npmjs.org/`, fresh repository-external cache/log/temp paths, and the resolved normal npm userconfig read-only: `npm ping --registry=https://registry.npmjs.org/`; `npm view kyw-dev --json --registry=https://registry.npmjs.org/`; `npm search kyw-dev --json --registry=https://registry.npmjs.org/`; `npm whoami --registry=https://registry.npmjs.org/`. Probe batch start: `2026-07-25T07:09:13.1999821Z`; retries: `0`.
- Executed exactly once: `node ./scripts/release-gate-isolation.mjs`. Start `2026-07-25T07:10:03.9083111Z`; end `2026-07-25T07:10:19.3317988Z`; runtime `15,420 ms`; exit `0`; runner attempts `1`; ambient retry `0`.
- Did not execute a model-backed marketplace command. The N/A basis and zero call/cost/retry provenance are recorded below.
- Final repository checks include exact refs/direct remote, tracked/untracked inventory, normal npm-userconfig metadata/hash, temporary-root absence, `git diff --check`, current-pair and all-Task canonical validators, format, package boundary, acceptance-to-test mapping, and manual complete-diff review.
- Forbidden commands/actions: `npm publish`, registry mutation, version change, Git tag, GitHub Release, public plugin submission, force/destructive recovery, workflow rerun, and automatic Task 0048 creation.
- Fresh 2026-07-26 preflight repeated the local/upstream/origin/direct-remote identity, clean tracked tree, exact two-path Task working set, Tasks 0041–0046 terminal/delivery ledger, PR #32 and post-merge exact-SHA CI, Task 0047 ref/PR/transaction/residue absence, package contract, package/plugin versions, lifecycle absence, tag absence, Release absence, and normal npm-userconfig hash/metadata checks.
- Two candidate wrapper constructions were rejected by execution policy before shell/process launch. They created no npm child and consumed none of the approved `npm run release:check` count. The subsequent direct command below is the only fresh composite execution; it was not a retry of an executed command.
- Fresh execution exactly once: `npm run release:check`. Start `2026-07-25T23:33:16.7314572Z`; end `2026-07-25T23:34:04.3481025Z`; runtime `47,614 ms`; exit `0`; npm `11.18.0`; retries `0`; model calls `0`. It used new repository-external empty user/global configs and cache/log/temp state, expanded only to `npm run release:ci && npm publish --dry-run --json`, and owned the one internal dry run.
- Fresh standalone `npm publish --dry-run --json` executions: `0`.
- Fresh registry probes, each exactly once against `https://registry.npmjs.org/` with its own new repository-external cache/log/temp state and the normal userconfig read-only: ping exit `0`, `1,216 ms`, `PONG`; view exit `1`, `1,125 ms`, `E404_PACKAGE_ABSENT`; search exit `0`, `1,868 ms`, 20 results and zero exact matches; whoami exit `1`, `1,353 ms`, `AUTH_REJECTED`. Retries/model calls were `0/0`; username and secret-bearing output were not retained.
- Fresh execution exactly once: `node ./scripts/release-gate-isolation.mjs`. Start `2026-07-25T23:36:16.7317350Z`; end `2026-07-25T23:36:30.7791153Z`; runtime `14,044 ms`; exit `0`; external invocations `1`; attempts `1`; status `CLEAN`; ambient retries `0`; model calls `0`.
- Fresh model-backed marketplace command executions: `0`. The reasoned N/A basis remained true and deterministic marketplace coverage came from the isolation gate.
- Fresh Task-owned external evidence/cache/log/temp root cleanup used an exact realpath/type/parent/leaf guard, removed only `kyw-task-0047-20260726-6c5d8f2a`, and confirmed it absent. The retained sanitized evidence path is `docs/tasks/0047-post-hardening-release-readiness-re-gate/TEST.md`.
- Post-evidence checks executed: current-pair canonical validation exited `0`; `npm run format:check` exited `0` over 282 UTF-8/LF text files; final status inspection found no staged/unstaged tracked path, only this pair untracked, no Git lock/Task transaction residue, and no remaining Task-owned temporary root.
- Blocker-resume preflight rechecked exact local/upstream/origin/direct-remote SHA, tracked tree/status, Tasks 0041–0046 status and Task 0046 exact delivery, Task 0047 pair/evidence hashes, package/plugin manifests and candidate inputs, refs/PRs/releases, Git/Task/temp residue, tool provenance, and normal npm-userconfig metadata/hash. No successful release, dry-run, ping, view, search, isolation, or model-backed command was repeated.
- One proposed authentication wrapper was rejected by execution policy before shell/process launch; it created no npm child and consumed no approved probe attempt.
- Executed exactly once after the matching post-login baseline check: `npm whoami --registry=https://registry.npmjs.org/`. Start `2026-07-26T00:05:22.0867185Z`; end `2026-07-26T00:05:23.1611560Z`; runtime `1,073 ms`; exit `0`; sanitized result `AUTHENTICATED`; retries/model calls `0/0`. Username, token, credential contents, and raw command output were not retained.
- Terminal local checks: canonical validation of all 47 Task pairs; `npm run format:check`; `npm run pack:check`; `git diff --check`; exact working-set/package-input inventory; secret-marker scan; AC-to-test cardinality and complete evidence/scope review.

## Results

- Fresh preflight passed at exact delivered `main` SHA `515a5ff5cc69f4324fcb10e48c7fb9c7f5fd9ca8`; Task 0046 exact PR/main runs are successful and canonical continuous dispatch selected Task 0047 with its dependency satisfied.
- Worktree evidence contains no staged/unstaged path and only this pre-created pair; Task 0047 has no branch or PR; transaction state is `NONE`; no Git tag or GitHub Release exists.
- Tool/package provenance: Git `2.51.2.windows.1`, Node `v24.11.0`, npm `11.18.0`, installed `codex-cli 0.145.0`, package `kyw-dev@0.1.0`. Active API model/effort remain honestly `UNAVAILABLE`.
- Approval-time preflight reconfirmed exact clean tracked `main` `515a5ff5cc69f4324fcb10e48c7fb9c7f5fd9ca8`, tracked-tree identity `32b25c6e850e356e0e169daab3715f10a77721b5`, only this pair untracked, Tasks 0041–0046 `DONE/PASSED`, exact merged PRs #27–#32, and 9/9 successful exact-head/main jobs for every delivery. Package identity/scripts, no-lifecycle boundary, no tags/Releases, and no Task 0047 branch/PR/transaction residue remained exact.
- Package contract before execution: `kyw-dev@0.1.0`, `private: false`, public npm registry/access, `release:candidate = node ./scripts/packed-release-check.mjs`, `release:ci = npm run check && npm run release:candidate`, `release:check = npm run release:ci && npm publish --dry-run --json`, no pack/publish lifecycle scripts, and `package.json` SHA-256 `44b55f2473987e05d97a5f18369492c75310d3bf58eb3445fee99cdd2b163b2e`.
- `npm run release:check` PASS: 286/286 tests in `41,702 ms`; lint over 69 JavaScript modules; format over 282 text files; package and candidate checks over 39 files and 92,971 packed bytes. The command exited `0`; no retry ran.
- Exact candidate identity: source SHA `515a5ff5cc69f4324fcb10e48c7fb9c7f5fd9ca8`; package `kyw-dev@0.1.0`; filename `kyw-dev-0.1.0.tgz`; file count `39`; packed size `92,971`; unpacked size `392,424`; archive SHA-256 `66afce883d5b990bfb6761cc464810b629c8db92bc3a60364461d967751e0ea6`; npm shasum `c1d55e346b7c1ae160937937681a09864e42b617`; npm integrity `sha512-jne8FZ4RmaZzZyGN+U0V/4O0gVpSsiSR1Rg6Ik7ok6N4eNphA0/k4B33zu3S4vP7WhlZMH62M6DPVlZgwDHtPQ==`.
- The candidate gate verified the exact allowlist, extracted contents, project and third-party legal notices, package hygiene, packed CLI, and all four packed Skills. The internal npm operation reported only dry-run notices, including login/publishing dry-run wording; it did not publish, reserve, or mutate the registry and is not interpreted as publication evidence.
- Registry probe observations, each with retry count `0`: `npm ping` exit `0`, runtime `1,248 ms`, result `PONG`; `npm view kyw-dev --json` exit `1`, runtime `884 ms`, observed `E404` package absence; `npm search kyw-dev --json` exit `0`, runtime `1,195 ms`, zero exact matches; `npm whoami` exit `1`, runtime `809 ms`, result `AUTH_REJECTED` from a 401/403-class response. No authentication username or secret-bearing output is recorded.
- `node ./scripts/release-gate-isolation.mjs` PASS: exit `0`, attempt 1 `CLEAN`, differences `0`, retry `0`, all guarded path checks passed, and the approved runner root was removed. The independently packed archive again had 39 files, 92,971 bytes, and SHA-256 `66afce883d5b990bfb6761cc464810b629c8db92bc3a60364461d967751e0ea6`.
- Isolation covered direct user/project install, update, doctor, normal-uninstall preservation refusal, force-uninstall owned-byte removal, preserved unknown/unrelated digests, and Codex marketplace add/discover/install/list/remove for exactly four Skills. Every expected lifecycle step completed with its contract exit status under child-only state. Installed Codex CLI provenance was `codex-cli 0.145.0`; model calls were `0`.
- Isolation protected-state sentinels were identical before/after: default npm userconfig entry count 1 and SHA-256 `f1cc16645bce996ea86ec4af6f4602de58b7b87e35c275e6fe51a043c396cff7`; normal agents entry count 5 and SHA-256 `7e30b31495f066683f96b33fe89420b57f3b11b5e26699aa3585119769e393c6`; normal Codex entry count 13,392 and SHA-256 `b798e19793394b63a1b836a4b58e40eeefd536cb4c2835df99997275cd64e464`. The runner reported `childOnly: true`, `parentUnchanged: true`, and `cleanup removed: true`.
- Normal npm userconfig before/after evidence remained exact without revealing its path or contents: length `74`, last-write time `2026-07-13T13:58:44.9273844Z`, SHA-256 `93cfd11938a716e0ac69c46c355fe2fe5f54903d78140d18890bd40ace3e049d`. No relevant auth environment variable was present before or after.
- Task-owned release and registry temporary roots were outside the repository. Two attempted PowerShell recursive-removal requests were rejected by execution policy before execution and mutated nothing; one guarded Node cleanup then verified exact realpaths, parent, and leaf patterns, removed both roots, and confirmed both absent. The repository contains no tarball residue.
- Reasoned model-backed marketplace result: `N/A`, not PASS. No supported dedicated release-marketplace model harness exists; the permanent current-byte release contract does not require a model-backed public admission gate; exact active model and reasoning effort are unavailable; nested `codex exec` is not a generic release requirement; and the user explicitly prohibited the command. Calls/cost/retries are `0/0/0`. Deterministic marketplace behavior was actually exercised by the isolation gate.
- Final before/after evidence reconfirmed HEAD/upstream/local `main`/`origin/main`/direct remote `main` all at the exact source SHA, zero tracked status entries, only the two expected untracked pair paths, no Git-operation residue, identical normal npm config metadata/hash, and both Task-owned npm temporary roots absent.
- Final static verification passed: the current pair and all 47 Task pairs passed the canonical validator; `npm run lint` passed over 69 JavaScript modules and foundation metadata; `npm run format:check` passed over 282 UTF-8/LF text files; `npm run pack:check` passed the unchanged 39-file/92,971-byte package boundary; and `git diff --check` passed. Full pair review found no stale active/TODO wording, scope drift, future-Task implementation, or unsupported PASS claim.
- Evidence location: this `TEST.md` is the retained sanitized command/result ledger, with the matching scope/verdict in `TASK.md`. Raw Task-owned npm caches/logs/temp archives were intentionally removed after identity and protected-state capture; no credential-bearing raw output is retained.
- Exact-candidate verdict: `BLOCKED`. Local Stable, candidate, package, dry-run, registry-name, and isolation evidence succeeded, but the required authentication boundary did not. This is not `READY_FOR_APPROVAL`, publication approval, or proof of an actually published package.
- Fresh 2026-07-26 preflight passed at exact source SHA `515a5ff5cc69f4324fcb10e48c7fb9c7f5fd9ca8` and tracked tree `32b25c6e850e356e0e169daab3715f10a77721b5`. Tasks 0041–0046 remained `DONE/PASSED`; PRs #27–#32 each retained 9/9 successful exact-head checks and a successful exact-merge-SHA `main` run; PR #32 remained runs `30147792841` and `30147881300`.
- Fresh composite result: PASS at the local/non-publishing boundary. It passed 286/286 tests in `37,749.7865 ms`, lint over 69 modules, format over 282 files, and package/candidate checks over 39 files and 92,971 packed bytes. The dry-run JSON identified `kyw-dev@0.1.0`, 392,424 unpacked bytes, shasum `c1d55e346b7c1ae160937937681a09864e42b617`, and integrity `sha512-jne8FZ4RmaZzZyGN+U0V/4O0gVpSsiSR1Rg6Ik7ok6N4eNphA0/k4B33zu3S4vP7WhlZMH62M6DPVlZgwDHtPQ==`; it did not publish.
- Fresh candidate and isolation boundaries independently agreed on filename `kyw-dev-0.1.0.tgz`, file count 39, size 92,971 bytes, and archive SHA-256 `66afce883d5b990bfb6761cc464810b629c8db92bc3a60364461d967751e0ea6`.
- Fresh registry result: the registry was reachable, the unscoped package remained absent by view and exact search, but authentication was rejected. The `E404` is an observed package-absence result rather than command-infrastructure failure; `AUTH_REJECTED` is the required blocker rather than N/A or PASS.
- Fresh isolation result: `CLEAN`, difference count 0, protected location count 3, target count 17, child-only environment true, parent unchanged true, cleanup removed true, direct lifecycle steps at their expected exit statuses, and local marketplace lifecycle passed for all four Skills using `codex-cli 0.145.0`.
- Fresh protected-state comparison passed. Normal npm userconfig length 74, creation/last-write metadata, `Archive` attribute, and SHA-256 `93cfd11938a716e0ac69c46c355fe2fe5f54903d78140d18890bd40ace3e049d` were identical. Isolation sentinels for default npm userconfig, normal agents, and normal Codex were byte/structure-identical before/after; repository refs/tree/status, package bytes, tags, Releases, and the user-owned pair before evidence recording were unchanged.
- Fresh exact-candidate verdict: `BLOCKED`. The repeated deterministic evidence does not supersede the failed mandatory authentication boundary, authorize publication, or permit `STANDARD` delivery.
- Fresh final evidence review found no product/configuration/permanent-document change, no introduced behavior or uncovered branch, no out-of-scope path, no secret-bearing retained evidence, and no unsupported PASS claim. Current pair validation and formatting passed after the evidence update.
- The authorized resume preflight again bound source SHA `515a5ff5cc69f4324fcb10e48c7fb9c7f5fd9ca8`, tree `32b25c6e850e356e0e169daab3715f10a77721b5`, package manifest SHA-256 `44b55f2473987e05d97a5f18369492c75310d3bf58eb3445fee99cdd2b163b2e`, and the retained 39-file/92,971-byte/archive-SHA evidence. The worktree had no tracked/staged change, only this pair untracked, and no Task-0047 ref/PR/transaction/tarball or Task 0048.
- Fresh provenance remained Git `2.51.2.windows.1`, Node `v24.11.0`, npm `11.18.0`, and installed `codex-cli 0.145.0`. Task 0046 PR #32, exact-head run `30147792841`, merge SHA `515a5ff5cc69f4324fcb10e48c7fb9c7f5fd9ca8`, and post-merge run `30147881300` remained successful.
- Intentional npm login changed the prior config state, so the current post-login state became the new protection baseline: length `74`, creation `2026-03-27T00:06:12.6838597Z`, last write `2026-07-25T23:57:41.8991567Z`, `Archive`, SHA-256 `ab0faa6a3ae63e78f89d8ca659b63d8910bee98f4bb9fabc6cd2ae147b769fb2`. Path identity and every recorded byte/metadata field were identical immediately before and after the approved probe.
- Authentication result: `AUTHENTICATED` with exit `0`. No username or secret-bearing output is retained. This clears the only recorded local readiness blocker and yields exact-candidate verdict `READY_FOR_APPROVAL`; it is not proof of publish permission, publication approval, npm publication, or any registry mutation.
- Terminal verification passed: 47/47 canonical Task pairs; format over 282 UTF-8/LF files; unchanged package boundary of 39 files and 92,971 bytes; whitespace/diff check; zero tracked or candidate-input changes; only this Task pair in the working set; zero credential-bearing markers; seven ACs mapped to eight rows.
- Final self-review found no product/configuration behavior, package byte, permanent-document, future-Task, or out-of-scope change. The repository verdict is complete and reproducible; exact-head PR and post-merge `main` evidence remains external mutable ledger state.

## Unverified

- Actual npm publish permission remains unverified; `npm whoami` establishes authenticated identity only and no publication or registry mutation is authorized.
- Task 0047 exact-head PR CI, merge, post-merge `main` CI, and external `STANDARD` ledger are intentionally not claimed in this repository-local snapshot; they must succeed before external delivery is satisfied.
- Actually published registry bytes, published-package licensing/identity, npm publication, tag, GitHub Release, and public plugin submission remain unverified and explicitly forbidden.
- No model-backed marketplace PASS is claimed. Its reasoned N/A applies only to this current contract and provenance; the deterministic non-model marketplace lifecycle did run and pass.
- Raw temporary logs/archive are not retained; durable evidence is the sanitized ledger in this pair and the reproducible identities above. The archive cannot be treated as a published artifact.

## Final Coverage Review

- [x] Compare the final diff to the matrix.
- [x] Map every acceptance criterion to one or more test rows.
- [x] Add coverage for introduced branches, failures, and compatibility behavior.
- [x] Confirm PASS evidence is reproducible.
- [x] Confirm required regressions ran.
