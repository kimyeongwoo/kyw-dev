# TEST 0004 — kyw-init Skill

## Status

PASSED

## Test Basis

- Task: `./TASK.md`
- Product requirements: `../../SPEC.md`
- Architecture constraints: `../../ARCHITECTURE.md`

## Intent-to-Test Matrix

| ID | Intent / acceptance criterion | Method | Level | Status | Evidence |
|---|---|---|---|---|---|
| T-01 | AC-01 empty-project initialization creates four project-specific documents without unresolved template artifacts | Validate the scenario contract, then run an isolated confirmed forward evaluation and inspect all outputs | Static / E2E eval | PASS | New evaluation created exactly four project-specific documents; original brief hash stayed fixed, AGENTS was 2,566 bytes, and placeholder/Task/unexpected-path counts were zero. |
| T-02 | AC-02 adoption preserves unrelated README/AGENTS sections while adding the missing durable contract | Hash/diff an existing-project fixture around an isolated confirmed forward evaluation | Integration / E2E eval | PASS | Adopt evaluation changed only the four durable paths, preserved both requested sentences verbatim, and kept package, source, and runbook hashes unchanged. |
| T-03 | AC-03 no document or directory write occurs before shared-understanding confirmation | Hash interrupted new/adopt/rebaseline evaluations before any confirmation | Integration / eval | PASS | Before confirmation, new remained 1/1 identical, adopt 5/5 identical, and rebaseline 5/5 identical through all interview turns; no added path was observed. |
| T-04 | AC-04 runtime, commands, components, and existing documentation facts are inspected rather than asked as decisions | Run a known-stack scenario and inspect the interview transcript against fixture facts | Static / eval | PASS | Evaluations used Node/runtime commands, port, source gap, storage conflict, and existing guidance directly; questions covered only unresolved interface/storage decisions and each included one recommendation. |
| T-05 | AC-05 generated AGENTS is thin, includes source routing and ordinary-small-change documentation impact, and handles the 4/8 KiB thresholds | Measure generated bytes and run Skill/output contract assertions | Static / integration | PASS | Static threshold/routing assertions passed; generated AGENTS sizes were 2,566, 2,918, and 2,413 bytes, all below 4 KiB. |
| T-06 | AC-06 only the four durable documents change; no application code or numbered Task is created | Compare full fixture file sets and hashes, and audit the repository diff | Integration / audit | PASS | Independent audits found exactly the four allowed changed paths in every scenario, zero unexpected paths, zero Task directories, and unchanged original application/support files. |
| T-07 | AC-07 new/adopt/rebaseline modes and intentional rebaseline conflict handling are explicit | Run mode table assertions and an interrupted rebaseline scenario | Static / eval | PASS | Static mode table passed; rebaseline identified the PostgreSQL/SQLite conflict, required intentional confirmation, and reconciled both to one confirmed JSONL design. |
| T-08 | Explicit-only metadata, canonical templates, remaining stubs, zero dependencies, and package contents do not regress | Run Skill validation, foundation tests, stable checks, and package inspection | Static / packaging / regression | PASS | Official Skill validation, 33/33 full tests, lint/format, exact 25-file pack check, and Node.js 22 passed; foundation kept only Task/audit as stubs and package metadata remained dependency/lifecycle-script free. |

Every acceptance criterion must reference one or more rows before the Task becomes `READY`. Add rows for meaningful behaviors discovered in the final diff.

## Regression Coverage

- [x] Existing README instructions remain usable.
- [x] Existing AGENTS content is preserved unless conflicting.
- [x] No numbered Task is created automatically.
- [x] `kyw-task` and `kyw-audit` remain non-mutating stubs.
- [x] Package remains dependency-free and lifecycle-script-free.

## Commands

Commands actually run:

- `git status --short --branch` (failed because Git metadata is absent)
- `npm run check` (pre-change baseline and preliminary implementation runs)
- `npm test -- --test-name-pattern="init|document"`
- `python C:/Users/DevHamster/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/kyw-init`
- `npm test`
- `npm run lint`
- `npm run format:check`
- `npm run pack:check`
- `npx --yes node@22 --test`
- `node --input-type=module -e "...validateTaskDirectory('./docs/tasks/0004-kyw-init-skill')..."`
- Context-isolated new/adopt/rebaseline forward evaluations against temporary fixture copies.
- PowerShell pre/post file-set, SHA-256, placeholder, mutation-boundary, preservation-marker, and AGENTS-byte audits for all three evaluation repositories.
- `git -c core.autocrlf=false diff --no-index --no-renames` name/status, stat, per-file content, and `--check` against the verified pre-change snapshot.
- PowerShell full-workspace file-set and SHA-256 comparison against the verified pre-change snapshot.

## Results

Record exact commands, exit status, and concise outcome. Do not mark a row PASS without evidence.

- Pre-change `npm run check`: exit 0; 26/26 tests passed, lint checked 14 JavaScript modules and foundation metadata, format checked 62 UTF-8/LF text files, and pack check matched the exact 25-file allowlist at 17,201 bytes. This establishes the baseline only and is not final Task evidence.
- Initial `npm test -- --test-name-pattern="init|document"`: exit 1; 12/13 selected checks passed. The sole failure was an assertion wording mismatch (`changed paths` versus the Skill's more precise `exact paths that changed`); no behavior defect was exposed. Node also auto-collected a fixture `.mjs`, so the non-test fixture source was renamed to an extensionless executable path before rerun.
- Initial official `quick_validate.py skills/kyw-init`: exit 1 before contract validation. Python 3.13 decoded with the Korean Windows `cp949` locale and rejected UTF-8 em dashes in three headings; the headings were changed to ASCII so the documented cross-platform command can be rerun unchanged.
- Preliminary `npm run check`: exit 1 during `npm test`; 32/33 tests passed. The foundation validator treated the literal `TODO` in a sentence prohibiting unexplained markers as an active placeholder. The wording was generalized without changing the materialization requirement; lint, format, and pack did not run in this chained attempt and are not claimed.
- Corrected `npm test -- --test-name-pattern="init|document"`: exit 0; all 12 selected file/contract checks passed, including seven `kyw-init` acceptance-specific tests and the canonical project-document contract.
- Corrected official `quick_validate.py skills/kyw-init`: exit 0, `Skill is valid!` with the documented command and no locale override.
- Preliminary successful `npm run check`: exit 0; 33/33 tests passed, lint checked 15 JavaScript modules and foundation metadata, format checked 74 UTF-8/LF text files, and pack check matched the unchanged 25-file allowlist at 19,970 bytes. These checks will be rerun after evaluation and terminal Task/Test updates.
- Context-isolated `new` evaluation: PASS. The first turn classified from the single brief, listed explicit unknowns, proposed only four writes, and requested confirmation. Pre-confirmation audit found 1/1 source files hash-identical and no additions. After confirmation, four documents were added, the brief remained hash-identical, AGENTS measured 2,566 bytes, and unexpected-path, placeholder, and Task-directory counts were zero.
- Context-isolated `adopt` evaluation: PASS. It inspected Node 22, commands, port 4318, current source behavior, and existing guidance; each turn asked one unresolved decision with a recommendation. All 5/5 files remained hash-identical before confirmation. After confirmation, exactly four durable paths changed, AGENTS measured 2,918 bytes, requested README/AGENTS sentences were preserved verbatim, and package/source/runbook hashes stayed fixed.
- Context-isolated `rebaseline` evaluation: PASS. It surfaced the PostgreSQL/SQLite conflict and refused the request for immediate rewriting until the JSONL decisions and final summary were confirmed. All 5/5 paths remained hash-identical before confirmation. After confirmation, exactly four durable paths changed, AGENTS measured 2,413 bytes, the import entry-point concept remained throughout the documents, and no placeholder, application, package, test, or Task path appeared.
- The isolated target repositories contained no application tests; their zero-test commands had zero failures but are not used as implementation evidence. These evaluations verify Skill behavior and document/mutation contracts only.
- Final post-evaluation focused tests and official Skill validation: both exited 0; all 12 selected checks passed and `quick_validate.py` reported `Skill is valid!`.
- Final stable commands: all exited 0. `npm test` passed 33/33; lint checked 15 JavaScript modules and foundation metadata; format checked 74 UTF-8/LF text files; pack check matched the exact 25-file allowlist at 19,970 bytes.
- `npx --yes node@22 --test`: exit 0; 33/33 tests passed with no skips on the minimum supported runtime.
- Final no-index review: name/status showed 6 expected modifications and 13 expected additions across 19 Task-scoped paths; stat and per-file content were reviewed, and `--check` reported no whitespace errors.
- Final independent hash audit: the first summary counted a `$null` from two empty missing-path pipelines as one item; the printed missing list was empty. The corrected audit passed with 68 before files, 81 after files, 6 expected modifications, 13 expected additions, 0 removals, 62 byte-identical existing files, and zero unexpected or missing paths.
- Initial direct Task-contract one-liner: exit 1 in the diagnostic wrapper because it incorrectly expected `{ valid, errors }`; `validateTaskDirectory()` actually returns an error array. The corrected array-based command exited 0 with `Task 0004 contract validation passed`.
- Post-terminal `npm run check`: exit 0 in the DONE/PASSED state; 33/33 tests passed, lint checked 15 modules and foundation metadata, format checked 74 files, and pack check matched 25 files at 19,970 bytes.

## Unverified

- Git working-tree/staging/history state remains unavailable because the supplied workspace has no `.git` metadata. Verified snapshots support content and file-set review but cannot recover repository history or staging state.
- Forward evaluations cover three representative repositories in fresh agent contexts, not every possible Markdown layout or every Codex UI surface. Arbitrary semantic merges remain reasoning-dependent; actual cross-surface plugin loading belongs to the later distribution/release Tasks.

## Final Coverage Review

Before marking this Test `PASSED`:

- [x] Compare the final diff to the matrix.
- [x] Map every acceptance criterion to at least one test row.
- [x] Add tests for newly introduced branches, error paths, or compatibility behavior.
- [x] Confirm recorded PASS claims are reproducible.
- [x] Confirm required regressions ran.

Coverage mapping is complete: AC-01 → T-01; AC-02 → T-02; AC-03 → T-03; AC-04 → T-04; AC-05 → T-05; AC-06 → T-06/T-08; AC-07 → T-07. Metadata, foundation-validator, README, fixture, and packaging changes map to T-01 through T-08, with no final-diff behavior outside the matrix.
