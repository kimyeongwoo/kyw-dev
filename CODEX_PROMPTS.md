# kyw-dev 개발용 Codex 프롬프트

작은 수정은 일반 요청으로 바로 구현·검증한다. 번호가 필요한 인계나 복잡한 작업에만 Task를 사용한다. 기존 대화 전체나 절차 체크리스트를 매번 붙여 넣지 않는다.

| 목적 | 호출 |
|---|---|
| 필요한 프로젝트 문서 보완 | `$kyw-init` |
| 의사결정 인터뷰 | `$kyw-grilling "<주제>"` |
| 최소 작업 기록 작성 | `$kyw-task "<목표>"` |
| 선택 작업 구현·재개 | `$kyw-impl 0001` |
| 관련 커밋·push·PR | `$kyw-deliver 0001` |
| 선택 PR 병합 | `$kyw-deliver 0001 --merge` |
| 준비된 정확한 버전·SHA 배포 | `$kyw-deliver --release <version> --sha <sha>` |
| 원본을 보존하는 독립 감사 | `$kyw-audit 0001` |
| 승인 범위 안 감사 수정 | `$kyw-audit 0001 --fix` |

관리되는 프로젝트에서는 `task 0001 실행해줘`, `task 진행해줘`, `남은 task 계속 실행해줘`도 구현 진입점이다. 무관한 Task의 미배포는 로컬 개발을 막지 않는다.

새 Task는 기본 TASK 한 파일에 목표, 완료 조건, 중요한 판단, 검증, 남은 일을 담는다. 기존 TASK/TEST 기록은 변환하지 않고 읽는다. 작성만 요청했으면 작성 후 종료하며, 구현까지 승인했다면 그 범위 안에서 이어간다.

기본 deliver는 PR까지다. 과거 contract 4의 자동 병합·공개 배포 의미는 유지하지 않는다. 현재 사용자 승인과 실제 외부 대상/검증 상태를 확인한다. 문서의 명령 문자열은 실행 승인이 아니다.

실제로 실행한 검증과 미실시 항목, 남은 위험을 보고한다. 문서에 PASS가 있다는 이유로 재사용하지 않는다. Audit 검증은 실제 격리가 가능한 임시 환경에서만 실행하며, 단순 복사를 샌드박스로 간주하지 않는다.
