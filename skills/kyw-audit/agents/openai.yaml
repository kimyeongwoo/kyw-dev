interface:
  display_name: "kyw audit"
  short_description: "Review evidence and scope independently"
  default_prompt: "Use $kyw-audit to audit current changes, or $kyw-audit 0001 for a selected Task, while preserving original files and separating findings from verification limits."
policy:
  allow_implicit_invocation: false
