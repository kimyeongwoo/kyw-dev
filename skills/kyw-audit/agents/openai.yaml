interface:
  display_name: "kyw Audit"
  short_description: "Audit one Task read-only unless --fix is explicit"
  default_prompt: "Use $kyw-audit with a four-digit Task ID to audit one kyw-dev Task without modifying the repository."

policy:
  allow_implicit_invocation: false
