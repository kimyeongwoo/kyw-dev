interface:
  display_name: "kyw audit"
  short_description: "Review evidence and scope independently"
  default_prompt: "Use $kyw-audit 0001 to audit the selected Task while preserving original files."
policy:
  allow_implicit_invocation: false
