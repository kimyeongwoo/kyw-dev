interface:
  display_name: "kyw Task"
  short_description: "Author complete dependency-aware Task sets"
  default_prompt: "Use $kyw-task to atomically author the smallest complete ready Task set, then stop."

policy:
  allow_implicit_invocation: false
