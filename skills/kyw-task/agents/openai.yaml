interface:
  display_name: "kyw task"
  short_description: "Author a minimal resumable work record"
  default_prompt: "Use $kyw-task \"goal\" to write a minimal Task record."
policy:
  allow_implicit_invocation: false
