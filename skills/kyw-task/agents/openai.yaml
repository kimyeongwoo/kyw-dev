interface:
  display_name: "kyw Task"
  short_description: "Create adaptive Task batches or execute one Task"
  default_prompt: "Use $kyw-task to atomically create the smallest ready Task set and optionally execute its first eligible Task."

policy:
  allow_implicit_invocation: false
