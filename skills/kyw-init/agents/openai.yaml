interface:
  display_name: "kyw init"
  short_description: "Maintain only useful project documentation"
  default_prompt: "Use $kyw-init to inspect and supplement this project’s needed documentation."
policy:
  allow_implicit_invocation: false
