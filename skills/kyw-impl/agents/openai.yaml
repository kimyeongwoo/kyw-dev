interface:
  display_name: "kyw impl"
  short_description: "Implement and verify an existing Task"
  default_prompt: "Use $kyw-impl 0001 to implement and verify the selected Task."
policy:
  allow_implicit_invocation: false
