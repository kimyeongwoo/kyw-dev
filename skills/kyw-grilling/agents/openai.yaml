interface:
  display_name: "kyw grilling"
  short_description: "Decision interview with delegated choices"
  default_prompt: "Use $kyw-grilling to resolve this design’s important decisions."
policy:
  allow_implicit_invocation: false
