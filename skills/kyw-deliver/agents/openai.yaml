interface:
  display_name: "kyw Deliver"
  short_description: "Deliver STANDARD through its release"
  default_prompt: "Use exact $kyw-deliver NNNN to resume STANDARD and its eligible npm→tag→Release sequence."

policy:
  allow_implicit_invocation: false
