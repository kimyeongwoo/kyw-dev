interface:
  display_name: "kyw deliver"
  short_description: "Prepare a PR, explicitly merge or release"
  default_prompt: "Use $kyw-deliver 0001 to prepare the selected Task PR."
policy:
  allow_implicit_invocation: false
