interface:
  display_name: "kyw Deliver"
  short_description: "Deliver STANDARD or exact opt-in public release"
  default_prompt: "Use $kyw-deliver NNNN for STANDARD; $kyw-deliver NNNN --public-release is exact npm→tag→Release resume."

policy:
  allow_implicit_invocation: false
