interface:
  display_name: "kyw Deliver"
  short_description: "Deliver one repository-complete STANDARD Task"
  default_prompt: "Use $kyw-deliver NNNN to perform, resume, or report the exact Task's STANDARD delivery."

policy:
  allow_implicit_invocation: false
