# Greeting Fixture

A dependency-free Node.js greeting library used to verify kyw-dev workflows in fresh Codex sessions.

## Usage

Import `greet(name)` from `src/greeting.mjs`. It accepts a non-empty string and returns `Hello, <name>!`.

## Development

Requires Node.js 22 or newer.

```bash
npm test
npm run lint
```
