# Greeting Fixture Specification

## Behavior

`greet(name)` accepts a non-empty string and returns exactly `Hello, <name>!`.

The exclamation mark is a product requirement. A period or missing punctuation is incorrect.
