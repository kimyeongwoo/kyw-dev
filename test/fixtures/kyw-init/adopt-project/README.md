# Signal Garden

Signal Garden receives local telemetry envelopes for development environments.

## Operator Notes

Production alerts use the `signal-garden` pager route.

The development listener uses port 4318 unless configuration overrides it.
